# Changelog

Cynos Rules 整套通用规则共用一个 SemVer 版本。目标项目把已完整合并的版本记录在 `docs/rules/VERSION`，本仓库在 `main` 为每个正式版本创建 `v<version>` tag。

## 未发布

README 改为英文默认入口，新增 `README.zh-CN.md` 中文版并提供语言切换；按快速开始、日常更新、规则导航、安全与版本重整内容。两版保留相同安装与安全约束，不翻译规则正文，不改变更新流程。

补强更新完整性检查：先列旧/新上游的完整规则差异，再核对临时与实际合并结果；不能仅验证已选择修改的文件，遗漏或冲突未解决时不提升版本。保留本地规则，不要求含本地修改的文件与上游完全相同。明确先核对落盘正文、再独立写入版本；首次安装入口留到最后，不创建临时版本号。

## 1.6.0

新增 `docs/rules/update.md`，集中原 README 的安装/更新流程，并通过托管入口识别自然语言请求。检查版本只读，安装或更新仍保留项目规则、按旧新上游与本地内容合并；README 改为首次接入短提示词。旧项目需升级一次入口，不新增 CLI、Skill 或自动检查。

## 1.5.0

明确 change 的完成边界：实施与必要验证完成并记录结果后，原三文件保留为历史；后续新增、调整或修复另建 change 并引用原需求，不继续扩写旧 Plan。完成后的提交、合并和发布事实留在 Git、PR、Release，不回填原 change。

补强修改前的需求定位与完成检查，要求本次实施的 Plan 与实际阶段状态、验证结果一致。

## 1.4.0

新建需求目录默认使用 `YYYY-MM-DD-<short-name>`，日期取首次创建日且保持不变；已有项目命名规则或稳定需求编号优先，旧目录不自动改名。

## 1.3.0

明确需求文档的读者、职责和细节边界：Intent 保留用户原意与必要项目补充，Spec 说明供人判断的方案，Plan 提供分阶段实施细则。

- 要求按需设置栏目、跨文件去重，避免重复抄写通用规则和堆积执行日志；
- 加入写前筛选、写后删减的方法及一组虚构短正例，展示取舍而非提供必填模板；
- Spec 聚焦本次关键方案与验收结果，逐项校验和测试矩阵留给 Plan；待定事项不得被擅自写成包含或排除；
- 保留影响方案判断的安全、兼容、接口和数据约定，不以精简为由删掉关键条件；
- 任务入口同时覆盖需求工件的编写与修改；本次需求三文件示范新写法；
- 不改变目录结构，不自动改写已有需求文档。

## 1.2.0

为 `AGENTS.md`、`CLAUDE.md` 等项目规则入口增加安全所有权边界：

- 根 `AGENTS.md` 使用带版本的 `cynos-rules:begin/end` 托管区块；
- 首次安装只插入完整区块，不改写、移动或格式化目标项目原内容；
- 未修改的旧 Cynos 区块可整体替换，本地修改通过旧 tag、新 tag 和目标内容三方合并；
- 1.0/1.1 等无标记旧版本只替换与旧 tag 逐字一致、连续且边界完整的 Cynos 章节，不以单行匹配判断所有权；
- v0、未知来源、额外规则文件和托管区块之外的内容默认保留；
- 标记损坏、重复、版本不一致或内容归属不明确时停止并询问用户；
- 全部规则成功合并后才同步区块版本和 `docs/rules/VERSION`。

## 1.1.0

新增默认适用于大部分用户沟通的 `communication.md`：

- 先给直接答案、判断或用户可观察结果，再按需补充理由、证据和实现细节；
- 删除寒暄、填充、重复结论、无信息标题和无价值工具过程叙述；
- 为一般说明、项目解释、方案、状态和故障提供按需使用的输出顺序；
- 压缩表达时保留否定、条件、数字、单位、版本、命令、精确错误和风险警告；
- 正式文档、代码、Commit、PR、Issue、报告及用户指定格式继续遵循自身规范；
- 只吸收 Cynos Guidance v0.1 和 Caveman 中与输出组织有关的核心思想，不引入对话工作流、品牌语气、强度模式、Skill、Adapter 或运行时。

## 1.0.0

首个正式的轻量规则版本：

- 提供简短 `AGENTS.md` 入口；
- 提供开发行为、架构、安全、评审、项目文件和 Git 六组通用规则；
- 约定 `docs/PROJECT.md`、需求 `intent/spec/plan` 和可选罗网场景测试目录；
- 约定正式分支、开发分支、短期分支和 Commit 类型，PR 策略由用户决定；
- README 提供由 AI 执行的安装、更新和旧文档整理提示词；
- 不再提供 npm、Pi Adapter、Harness Adapter、Runtime 注入、Manifest 或评测体系。

### 从 v0 迁移

v0 是对未带 `docs/rules/VERSION` 的旧 `java-taro-rules` 类项目规则的兼容称呼，不是本仓库的正式 Release。

迁移到 1.0.0 时：

- 将可以通用化的行为、架构、安全和评审规则与 1.0.0 合并；
- 保留仍有效的 Java、Taro、前后端、数据库、部署、命令、模板和协作约定；
- 保留目标项目已有的额外规则文件；
- 冲突由 AI 列出并交给用户决定，不能整目录覆盖；
- 全部合并成功后才写入 `docs/rules/VERSION = 1.0.0`。

## 版本规则

- **Major**：改变规则职责、文件合同或需要人工迁移；
- **Minor**：向后兼容地增加规则或安装能力；
- **Patch**：不改变规则意图的修正、去重和澄清。

更新 AI 应比较目标版本对应 tag 与目标版本之间的上游差异，再合并目标项目本地修改。不得只根据版本号覆盖文件，也不得自动降级。
