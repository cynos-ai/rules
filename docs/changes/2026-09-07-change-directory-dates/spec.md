# 日期前缀命名方案

新建需求目录默认使用 `YYYY-MM-DD-<short-name>`，例如 `2026-09-07-change-directory-dates`；它仍是原有的 `<change-id>`，内部 intent/spec/plan 三文件布局不变。

日期取需求目录首次创建日，不是修改日或发布日期；后续修改、延期和发布不更新日期。主题名简短且有辨识度，不能只用日期。

这是默认约定：项目已有明确命名规则或稳定需求编号时优先沿用，不强制叠加日期。已有目录及引用保持原样，不自动补日期或改名。
