# 修复与复测

## 一、补强更新核对

在 update.md 第 7 步明确先列旧/新上游规则差异，再与本地合并；第 8 步按全量差异核对临时与落盘内容，版本最后同步。保留本地所有权、只读检查、异常停止和授权边界。CHANGELOG 追加未发布修复说明，不改已有双语 README、PROJECT、AGENTS 或版本。

## 二、实际操作与静态检查

复用上轮新安装和旧版升级的业务初始内容、英文短提示词、工具及判定口径，仅替换候选更新文档；另回归只读检查和实质冲突。每例一次独立 openai-codex / gpt-5.6-luna / max 运行，核对实际请求配置，不重跑挑成功。

核对逐文件结果、读取/写入轨迹、入口外内容、本地补充与历史保护，不仅检查版本和模型最终回复。继续使用可写的隔离项目与离线 GitHub 镜像；不把模拟源当成真实发布网络验证。

所有脚本、输入和证据放 /tmp；保存原失败与本轮基线 hash，完成后检查 Markdown、链接、精确修改范围及冻结记录。先保存结果，再记录本次 Plan 状态。单轮通过不等于长期遵循率，提交发布留待测试结论后处理。

## 三、独立版本步骤与时序复测

用户同意继续。第 6、7 步明确只准备临时结果，第 8 步依次执行正文落盘、实际内容核对、独立版本写入及回读；区分已有旧标记与首次安装入口，不新增版本格式。

新一轮沿用已补齐归档接口的隔离工具，首次安装、旧版升级、只读和冲突各运行一次指定模型。运行前固定时序门槛：版本提升前已实际读取并成功核对落盘正文；旧版保持原标记至该核对完成，首次入口与 VERSION 均延后；最后版本步骤不夹带规则正文修改。结合工具内命令顺序与工具后快照核对，不能只以工具调用边界推断内部写入顺序。保留原轮次证据，不重跑选择成功。

## 完成记录

2026-09-07：三个阶段实施和本次必要验证完成。前轮修复了漏合并，但新安装遭遇归档替身缺口，补齐接口后另测一次；人工发现版本提前写入，未据文件检查通过关闭需求。这 5 次任务、57 次请求的失败、补测与时序问题仍保存在 `/tmp/cynos-update-completeness-Ixj1Oa/report.md` 和 `/tmp/cynos-update-archive-check-PdwWqa/`。

明确独立版本步骤后，新一轮四例各一次、48 次指定模型/max 请求，逐文件和时序核对通过。首次安装先验证落盘规则与原入口未变，再写入口和 VERSION；旧版先核对全部上游差异、验证已写正文且保留旧标记，再独立写版本。未变化文件此前已比对且没有写入，最终另做全量回读。快照确认版本阶段没有夹带正文改动；上轮提前写版本的轨迹作为负例被拒绝。两次模型校验脚本错误保留，在同一任务修正，不是任务重跑。

静态检查通过：43 份 Markdown、39 个相对链接；旧双语文档、已完成需求、原失败证据和阅读副本不变。新报告：`/tmp/cynos-update-order-FxQFDJ/report.md`，对应 `validation-all.json` 与 `sequence-validation.json`。未测试真实网络、强制中断/版本写入失败恢复、删除改名或无标记迁移，不证明跨文件原子性或长期遵循率；fixture 短期分支遗漏未扩修。完成时未提交或发布，后续事实不回填本需求。
