# 规则正文改为英文

用户希望将各规则从中文改为英文，并确认只维护英文正文，不另建中文规则副本。仅转换语言，不改变规则含义；翻译 `docs/rules/*.md` 和入口托管区块，README 继续中英双版，历史已完成需求不翻译。“按用户主要语言回复”保持不变。

用户后续确认：先澄清“保留本地内容不等于解决冲突”的已有要求，兼容本地补充仍正常合并；检查模型连接后，对首次安装单独补测一次，原失败保留，再次失败则停止，不反复跑到成功。

用户再次确认：明确文本合并冲突不等于语义冲突，避免兼容的本地补充被过度拦截。

项目补充：当前正式版本为 v1.6.1，上一需求 `2026-09-07-rules-update-completeness` 已完成。本次承接其完整差异合并和版本最后写入要求，但不调整更新行为，也不自动提交或发布。
