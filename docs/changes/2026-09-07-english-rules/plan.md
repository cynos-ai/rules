# 实施与验证

## 一、翻译与语义核对

逐份翻译八份规则和 AGENTS 托管区块；更新两版 README、PROJECT 的语言说明及未发布 CHANGELOG，版本暂留 1.6.1。保持更新 15 步顺序、需求三文件职责与完成边界、安全和用户授权条件；同步英文标题链接并保留已有完成边界锚点。

逐条对照正式版本的句段和示例，检查否定、强度、顺序、例外、路径和固定标识。检查 Markdown、链接、修改范围与保护文件 hash；使用无会话历史的指定模型独立检查语义及中文回复行为。

## 二、真实工具复测

复用隔离 fixture、归档接口和正式 tag 替身，保存本轮输入、差异与验收标准后再运行。覆盖首次安装、中文旧版到英文升级、只读检查、实质冲突，使用 openai-codex / gpt-5.6-luna / max，逐请求核实配置，各场景只跑一次，不降级或重跑挑成功。

从完整上游差异核对最终规则、本地补充、入口外内容和历史文件。人工审阅工具内部命令与文件快照，证明正文实际核对后才更新入口版本和 VERSION，冲突或只读场景不写入。另区分语言翻译和真正语义冲突，不能误删本地中文规则。

### 已授权的针对性补测

2026-09-08：用户同意先澄清 update 第 7 步中本地保留与实质冲突决定的区别，再对最终候选验证。先做一次指定模型连接探针；通过后，干净首次安装、实质冲突及保留兼容中文本地补充的旧版升级各一次。只读检查沿用上一轮结果，不重复跑已无相关变化的场景。所有旧结果和新输入差异保留，不把网络失败改判成功，再失败就停止本轮该场景，不自动重试。

证据目录为 `/tmp/cynos-conflict-confirmation-GRYKcO/`。静态核对限制本轮只改第 7 步、当前需求和未发布说明；执行仍检查每次请求配置、完整文件与实际落盘核对早于版本写入的时序。连接探针不等于后续可用性保证。

### 文本与语义冲突的进一步区分

2026-09-08：用户确认继续处理过度拦截。把判据前移到安装/更新编号步骤之前，同时覆盖入口和规则文件，替换上一轮第 7 步单一说明；不只在规则文件阶段解释，以免入口文本合并已提前停下。

证据目录 `/tmp/cynos-semantic-merge-gnNcer/`。只运行兼容本地补充的旧版升级与实质冲突两例，各一次，模型与隔离方式不变，保留全部旧结果与新输入对照。首次安装沿用上一轮已正常完成且时序核对通过的结果，不再次运行；只读沿用原检查模式通过记录，明确组合证据范围，不声称四例都在同一候选上重跑。检查全文、所有权、版本时序，并保持真正冲突及不确定性需确认的门槛。

## 三、收尾

证据与脚本只留 `/tmp/cynos-english-rules-OMIAhd/` 或其引用的新临时目录，不加入项目工具。先保存报告，再依据实际结果更新本 Plan；未满足验收保持进行中，不自动提交发布。

## 完成记录

2026-09-08：三个阶段及本次必要验证完成，采用下述明确范围的组合证据，不声称所有场景在同一最终候选上重跑。原目录创建日保持 2026-09-07；完成后本需求三文件保留为历史。

第一轮独立审查发现 refactor 限定漏译、review 的已有需求表达和 project-layout 的约束措辞问题，已按原文修正；中文语言探针也出现 PROJECT 更新解释错误，未算全面理解通过。修订后的独立读者确认关键修订，正确解释 PROJECT 检查/更新边界并用中文回答。作者与静态核对覆盖九份受管内容的 444 个非空行对、46 份 Markdown 和 39 个相对链接，不能据结构相同代替语义证明。

上一候选的旧版升级和只读通过，但首次安装末端 fetch failed、实质冲突未询问就升版，两项失败均保留。对应两轮共 11 次任务、152 请求；首轮检查器漏认“英语”只修验证器，原 failed 结果未覆盖。报告：`/tmp/cynos-english-rules-OMIAhd/report.md` 与 `/tmp/cynos-english-rules-final-bSep6p/report.md`。

用户确认后的首轮澄清与补测共 4 次任务、42 请求：连接探针、首次安装正常完成及其时序、实质冲突停止通过，但兼容中文补充升级被文本合并 hunk 冲突拦截，未算通过。原报告与失败保留在 `/tmp/cynos-conflict-confirmation-GRYKcO/report.md`，不改判旧 fetch failed 或声称已定位网络故障原因。

再次确认后，判据前移到入口和规则文件合并之前，明确文本重叠先核对含义，确认兼容才临时重建并继续；矛盾或无法确认仍等待用户。最终两例各一次、29 请求：兼容升级完整保留英文上游和中文本地补充；真正冲突先询问，文件和版本全程不变。人工与快照确认实际正文核对后才分别写入口版本和 VERSION。两次被 shell 零退出码掩盖的内部校验失败在同任务修正并保留，版本没有提前提升。

最终报告 `/tmp/cynos-semantic-merge-gnNcer/report.md` 及 coverage.json、validation-execution.json、sequence-validation.json 记录组合范围：本轮升级/冲突、前轮首次安装、原检查模式只读结果。累计 17 次任务、223 次请求，均为指定模型/max，包含所有旧失败，不表示全部通过；无降级、超时或同一候选反复重跑挑成功。最终静态检查覆盖 46 份 Markdown、39 个相对链接，旧证据与历史文件 hash 不变。

真实公网、中断/版本写入失败恢复、原子性、删除改名、跨平台和长期稳定性未验证；fixture 短期分支遵循未扩修，不声称全部 Rules 每次均被遵循。完成时版本仍为 1.6.1，未提交或发布；后续提交、合并和发布另行授权，其事实不回填本需求。
