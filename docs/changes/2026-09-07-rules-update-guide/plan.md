# 实施与验证

## 一、集中流程并接入路由

将 README 安装提示词的执行步骤移入 `docs/rules/update.md`，保留旧版本识别、三方合并、异常停止、平台入口、PROJECT、Git/PR 和旧文档整理的条件与例外。新增只读检查模式、正式 Release 定位和目标固定要求。

更新 AGENTS 托管区块的任务路由及意图说明；README 改为短提示词，说明旧项目的一次接入。同步 PROJECT、默认规则目录示意和未发布 CHANGELOG；不改版本、不改历史 change、本地副本或其他仓库。

## 二、验证与收尾

在 `/tmp` 保存原 README 流程、旧工件和副本 hash；核对迁移后原执行步骤没有遗漏，检查 Markdown 围栏、相对链接、路由目标、修改范围与版本。

人工逐项走查：只读检查、明确更新、近似拼写、其他产品 rules、缺失文档的旧版接入、正式 Release 不可用、本地版本领先、三方冲突和损坏标记。核对检查不写文件、目标不漂移、冲突不提升版本、历史不回写。

本轮不做跨平台真实会话验收，不把静态与人工走查写成模型执行通过；保存结果后记录完成状态。提交和发布不在本次授权内。

## 完成记录

2026-09-07：两阶段完成。新增更新文档和入口路由，README 只保留短提示词；同步项目说明、目录示意与未发布说明，版本未改。

静态检查通过：9 个修改/新增文件，36 份 Markdown、15 个相对链接；原 15 个执行步骤除固定 tag 与澄清本地版本比较两处外逐字保留。只读检查、旧版接入、异常停止、冲突与恢复等 11 项作者人工走查完成，旧 change 与本地副本 hash 不变。证据：`/tmp/cynos-update-guide-xMD4A1/report.md`、`validation.json`。

未执行真实 AI 会话和跨平台验证，不承诺实际触发稳定性；本次只完成文档与路由，不包含提交或发布。
