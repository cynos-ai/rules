# 完成边界实施与验证

## 一、补齐规则

在 `docs/rules/project-layout.md` 的需求工件章节加入 [Spec](spec.md) 的完成与后续变更边界；在 `docs/rules/behavior.md` 修改前检查中增加引用，避免修 Bug 等任务漏读；CHANGELOG 记录为未发布。保持 communication、AGENTS 入口、版本及既有 change 不变。

用独立模型检查：原范围未完成、完成后的新增/调整/修复、仅部分阶段完成、完成后的纯发布，以及新旧 change 的关联方向。核对原三文件不会因后续任务被改写，也不会把必要验证未完成误判为完成。

## 二、检查实际回复

摘录本对话六组自然语言回复，保留任务背景、顺序和适用限制；排除工具日志，不把整理的背景描述冒充用户原话。使用 `gpt-5.6-luna / max` 按现有沟通规则评审原回复、提出必要改写，再由新会话复核建议是否丢失事实或改变含义。

记录样本中的冗余类型及执行改进；不以字数或单次模型评分宣称普遍遵循率。样本、提示词、原始输出和复核结果保存在本地 `/tmp/cynos-change-completion-tQw4Gp/`，不纳入 Git。

结束前检查 Markdown、链接、版本、模型快照与修改范围，并静态核对 AGENTS → behavior → 完成边界的读取路径；确认所有旧 change、communication 和阅读副本内容未变，再在本 Plan 记录完成结果。

## 完成记录（2026-09-07）

本次实施与验证已完成。`gpt-5.6-luna / max` 独立检查的八类完成边界判断及新旧关联方向均符合预期；新增的 behavior 读取引用由静态检查覆盖。

六组实际回复中，独立评审判五组符合、一组需去掉未请求的微信连接细节。它未支持把发布进度整体判为冗余：阶段变化和影响验证范围的信息应保留。此前对过程播报的自评据此收窄，不追加沟通条款。新会话复核唯一改写及其他保留建议，未发现重要事实丢失；这只是样本检查，不是普遍遵循率或长期执行效果证明。

32 份 Markdown、18 个相对链接、版本、文件范围和快照检查通过；旧 change、沟通规则、AGENTS 和阅读副本未变。完整检查说明位于上述本地目录的 `report.md`，完成时尚未提交或发布。
