# 让需求三份文档各有重点

## 用户诉求

Intent 应保留用户原始意图，以及必要的系统和项目补充；Spec 可以是基于需求的方案，但不要展开过多细节；Plan 才是 AI 可以分阶段实施的细则。三份文档都要让人读得出重点，不要长、杂、重复。关键不是单纯缩短篇幅，而是每段有具体信息，不靠填满栏目显得完整。

## 项目补充

罗网的部分需求文档把实现机制写进 Intent，又在 Spec 和 Plan 重复展开。本次调整前，Rules 只简述三份文件包含什么，缺少读者、细节边界和去重要求。

用户已同意先调整 Rules；罗网历史文档不在本次修改范围。后续同意聚焦 Spec、保护待定事项，并用一组短正例及写前筛选、写后删减的方法改善效果；测试指定使用 `gpt-5.6-luna / max`。用户已同意用罗网真实需求的独立副本验证写法，并在阅读后同意收敛重复规则；保留取舍方法和短正例，不追加模板、不覆盖罗网原文。

用户随后授权推送并正式发布。
