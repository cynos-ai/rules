# 需求文档规则实施计划

## 阶段一：规则正文

- 修改 `docs/rules/project-layout.md` 的“需求工件”章节，落实 [Spec](spec.md) 中的职责和细节边界。
- 补充按需组织、跨文件去重及进度记录要求，不改动其他文件布局约定。

验证：用“用户指定技术约束”“公共接口即需求”“内部实现细节”三类情况检查边界是否明确。

## 阶段二：入口和说明

- 调整 `AGENTS.md` 的任务路由，覆盖需求工件的编写和修改。
- 更新 README 的规则简介，在 Changelog 增加未发布记录；保留 VERSION 和托管区块版本。

验证：入口指向规则正文，不复制整套写作要求；版本标记仍一致。

## 阶段三：检查

- 检查 Markdown 相对链接、代码围栏、托管标记和 `git diff --check`。
- 对照 Spec 复核新规则和本目录三文件，确认没有重复扩写、历史文档修改或罗网仓库变更。

首轮对照仅证明部分改善，详见本地 `/tmp/cynos-requirement-ab-msIpIa/report.md`。

## 阶段四：收紧规则并复测

- 按 Spec 补强关键方案聚焦和待定事项边界，保留本次修改前的规则快照。
- 使用用户指定的 `gpt-5.6-luna / max`，对相同两组输入分别按本次修改前、后的规则生成文档；不与其他模型的结果混算。
- 独立盲评同时提供两版共同收到的项目规则，核对待定项、重点、重复、Plan 可执行性和关键约束；记录实际模型、调用结果、全文与篇幅，不以字数单独判优。

阶段四结果：待定项保护在配置样例上改善，Spec 精简未获证明；场景组盲评超时。详见本地 `/tmp/cynos-requirement-luna-wlmYBK/report.md`。

## 阶段五：短正例与内容取舍

- 在 project-layout 中加入三文件短正例及写前筛选、写后删减方法；正例使用不同于测试输入的虚构需求，不规定固定字数或必填栏目。
- 核对上一轮同模型、同 max 参数的已保存输出及规则 hash，复用为本轮基线；对相同两组输入生成新稿，保留全部结果。
- 用独立读者会话查找诉求、方案和首阶段行动，复核关键要求、待定项及示例内容是否被误带入；以可读性与完整性为主，篇幅为辅。

阶段五结果：`gpt-5.6-luna / max` 两次新生成、两次独立读者检查均完成；读者对配置组判持平，对场景组偏好新版，均未发现关键要求丢失或待定项定案。Spec 分别减少 1.0% / 19.5%，配置稿仍偏细；小样本不代表普遍效果。本地全文、基线复用说明及局限见 `/tmp/cynos-requirement-example-beJ6iJ/report.md`。

## 阶段六：真实副本验收与规则收敛

- 已改写罗网场景质量需求的独立副本，保存在 `deliverables/luowang-scenario-design-quality/`；核对当前代码落点及历史状态，未覆盖原文或复跑罗网工程测试。独立 `gpt-5.6-luna / max` 读者检查指出的两处约束已补回；输入、输出与修订检查见本地 `/tmp/cynos-real-document-reader-gEd7iJ/`。
- 用户阅读后反馈：“可以，好多了，之前全是套话的感觉。”这证明该副本的可读性获得认可，不代表罗网产品验收或规则对所有需求都有效。
- 据此合并 Spec/Plan 的重复分工说明、通用规则引用及状态记录要求，将套话删减和关键条件保护集中到写后检查；保留原有筛选问题与通知短正例，不增加模板或扩大需求范围。

验证：比较收敛前后的关键约束，确认三文件职责、待定保护、条件与例外、公共接口和安全边界、分阶段实施与未验证状态均保留；核对筛选问题、短正例及需求章节之外的内容未变。检查 Markdown、链接、托管标记、版本与修改范围。收敛检查材料保存在本地 `/tmp/cynos-rule-consolidation-uFgWRV/`，不纳入规则交付。

当前状态：规则收敛完成。`gpt-5.6-luna / max` 的一次独立语义检查及七个边界问答未发现关键约束丢失或新增强制要求；静态检查通过，筛选问题、短正例、其他规则章节、罗网原文和已验收副本均未变。本轮未重跑文档生成对照，不将语义检查当作新的效果证明。版本仍为 `1.2.0`，尚未提交或发布。
