# 需求定位与 Plan 收尾修复计划

## 一、调整入口

在 `docs/rules/behavior.md` 将修改前的条件式提醒改为先查找相关需求、读取三文件，再按既有完成边界判断；保留代码与测试勘察要求。在 `docs/rules/review.md` 增加本次 Plan 与实际结果一致的检查，并保留历史 change 不回写的边界。CHANGELOG 追加未发布说明。

不改 project-layout、communication、AGENTS、版本或既有 change，也不处理首轮观察到的其他 Git 或写作顺序问题。

## 二、原场景复测

首轮材料在 `/tmp/cynos-change-execution-Uhbj2Z/`。复用其三个场景、业务文件、任务提示、工具接口和判定口径，只替换候选 behavior/review；在新的 `/tmp/cynos-change-followthrough-BLuIFu/` 创建测试仓库，不覆盖首轮。

仍用 `openai-codex / gpt-5.6-luna / max`，每场景一次独立运行，不在提示中给出新建或续写答案。核对模型及实际请求参数、入口读取、原 Plan 的读取与收尾、文件 hash、独立功能验证和是否发生未经授权的 Git 操作。

模型执行结束后先保存并检查测试结果，再完成本 Plan 的状态记录。静态检查覆盖链接、版本、修改范围和原 change/首轮证据未变；失败如实保留，不重跑挑选通过结果。

## 完成记录（2026-09-07）

实施与约定验证已完成，三个场景本轮均通过。原失败场景在修改代码前读取了原三文件，完成 CLI 3 项及全套 6 项测试后，将同一 Plan 的“待完成”替换为完成与验证记录。另两个场景仍保留已完成的旧三文件；独立函数与 CLI 复核通过。

模型及实际请求参数符合预定配置，每例只运行一次；输入对照确认业务文件、任务提示不变，仅 behavior/review 更新。35 份 Markdown、20 个相对链接、版本和修改范围检查通过，旧 change、阅读副本及首轮失败材料未变。

完整结果及未处理的 Git 分支遵循问题见上述本地目录的 `report.md`。完成时尚未提交或发布。
